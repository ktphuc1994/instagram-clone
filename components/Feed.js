import Stories from './Stories';

export default function Feed() {
  return (
    <main>
      <section>
        {/** Story */}
        <Stories />
        {/** Post */}
      </section>

      <section>
        {/** Mini Profile */}

        {/** Suggestion */}
      </section>
    </main>
  );
}
